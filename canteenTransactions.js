// Placeholder for canteen transaction service
// Replace with real implementation as needed

export function saveCanteenTransaction(transaction) {
  // Simulate saving transaction (e.g., to server or local storage)
  console.log('Saving canteen transaction:', transaction);
  return Promise.resolve({ success: true });
}
